name: Nirwana Music CI/CD

on:
  push:
    branches: [ "1.0.1" ]
  workflow_dispatch:

jobs:
  build_and_cleanup:
    name: Build and Purge
    runs-on: ubuntu-latest

    steps:
      # 1. Setup Environment
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 2. Private Clone
      - name: Clone Nirwana-Music
        run: |
          git clone --depth 1 --branch 1.0.1 https://${{ secrets.REPO_ACCESS_TOKEN }}@github.com/iamzeus14/Nirwana-Music.git .

      # 3. Send Trigger Notification
      - name: Send Trigger Notification
        run: |
          COMMIT_HASH=$(git rev-parse --short HEAD)
          COMMIT_MSG=$(git log -1 --pretty=%B | head -n 1)
          TIME=$(date +"%a %b %d %I:%M:%S %p")
          
          # Formatting: Headers are Bold, Values are Monospace, Commit hash is normal
          MESSAGE="<b>Build Triggered ⌛</b>%0A"
          MESSAGE+="<b>App : </b><code>Nirwana Music</code>%0A"
          MESSAGE+="<b>Support : </b><code>Android 13.0 - 16.0</code>%0A"
          MESSAGE+="<b>Variant : </b><code>Released</code>%0A"
          MESSAGE+="<b>Machine : </b><code>Ubuntu 24.04.3 LTS</code>%0A"
          MESSAGE+="<b>Cores : </b><code>4</code>%0A"
          MESSAGE+="<b>Time : </b><code>$TIME</code>%0A"
          MESSAGE+="<b>Top Commit : </b>$COMMIT_HASH <code>$COMMIT_MSG</code>"

          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="$MESSAGE" \
          -d parse_mode="HTML"

      # 4. Prepare Gradle
      - name: Prepare Gradle
        run: |
          if [ ! -f gradlew ]; then gradle wrapper; fi
          chmod +x gradlew

      # 5. Build the Music App
      - name: Build APK
        run: ./gradlew assembleDebug --stacktrace 2>&1 | tee build_log.txt

      # 6. Upload & Send Link
      - name: Upload and Send Link to Telegram
        if: success()
        run: |
          APK_PATH=$(find . -name "*.apk" | grep "debug" | head -n 1)
          APK_NAME=$(basename "$APK_PATH")
          
          # Using transfer.sh to handle the 86.5MB file
          DOWNLOAD_URL=$(curl --upload-file "$APK_PATH" "https://transfer.sh/$APK_NAME")
          
          MESSAGE="<b>Build Completed ✅</b>%0A%0A"
          MESSAGE+="<b>App : </b><code>Nirwana Music</code>%0A"
          MESSAGE+="<b>File : </b><code>$APK_NAME</code>%0A"
          MESSAGE+="<b>Size : </b><code>86.5 MB</code>%0A"
          MESSAGE+="<b>Variant : </b><code>Released</code>%0A%0A"
          MESSAGE+="<b>Download Link : </b>%0A$DOWNLOAD_URL"

          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="$MESSAGE" \
          -d parse_mode="HTML"

      # 7. Error Handling
      - name: Send Error Log on Failure
        if: failure()
        run: |
          [ -f build_log.txt ] && tail -n 200 build_log.txt > error_log.txt || echo "Failed" > error_log.txt
          curl -F document=@"error_log.txt" \
          "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument" \
          -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -F caption="❌ <b>Build Failed on 1.0.1</b>" \
          -F parse_mode="HTML"

      # 8. Cleanup
      - name: Purge Workspace
        if: always()
        run: |
          rm -rf ./*
          rm -rf .git
