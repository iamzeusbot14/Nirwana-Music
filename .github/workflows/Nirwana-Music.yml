name: Nirwana Music CI/CD

on:
  push:
    branches: [ "1.0.1" ]
  workflow_dispatch:

jobs:
  build_and_cleanup:
    name: Clone, Build, and Purge
    runs-on: ubuntu-latest

    steps:
      # 1. Environment Setup
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 2. Manual Clone of the Private Repo
      - name: Clone Nirwana-Music
        run: |
          git clone --depth 1 --branch 1.0.1 https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/iamzeus14/Nirwana-Music.git .
          echo "✅ Repository cloned successfully."

      # 3. Handle Gradle Wrapper (Generates it if missing)
      - name: Prepare Gradle
        run: |
          if [ ! -f gradlew ]; then
            echo "⚠️ gradlew not found in repo. Generating..."
            gradle wrapper
          fi
          chmod +x gradlew

      # 4. Build the Music App
      - name: Build APK
        run: |
          ./gradlew assembleDebug --stacktrace 2>&1 | tee build_log.txt

      # 5. Send APK to Telegram
      - name: Send APK to Telegram
        if: success()
        run: |
          APK_PATH=$(find . -name "*.apk" | grep "debug" | head -n 1)
          curl -F document=@"$APK_PATH" \
          "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument" \
          -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -F caption="🎵 *Nirwana Music Build Success!*%0A📌 *Branch:* 1.0.1%0A👤 *By:* ${{ github.actor }}" \
          -F parse_mode="Markdown"

      # 6. Send Error Log on Failure
      - name: Send Error Log
        if: failure()
        run: |
          [ -f build_log.txt ] && tail -n 200 build_log.txt > error_log.txt || echo "Clone or Build failed before logs were written." > error_log.txt
          curl -F document=@"error_log.txt" \
          "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument" \
          -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -F caption="❌ *Nirwana Music: Build Failed*%0ACheck the attached logs for errors." \
          -F parse_mode="Markdown"

      # 7. Final Purge (Delete all files from runner)
      - name: Purge Workspace
        if: always()
        run: |
          rm -rf ./*
          rm -rf .git
          echo "✅ All artifacts and code deleted from runner."
